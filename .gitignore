node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
coverage

# Credentials for the audio generator. Never committed, never bundled.
.env
.env.*
!.env.example

# Where the production deploy target lives. The example is committed; the real
# file names a host and a private key path, so it is not. See docs/DEPLOYMENT.md.
!.env.deploy.example
*.serviceaccount.json
gcp-*.json
google-credentials*.json

# Written by `npm run generate-audio`.
audio-report.json

# The sync server's own copy of the vocabulary, and the shared token that
# guards it. Personal data and a secret — neither belongs in the repository.
server/data/

# TypeScript incremental build cache
*.tsbuildinfo
